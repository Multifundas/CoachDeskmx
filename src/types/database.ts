// Tipos de base de datos.
// Fase 1: placeholder. Se regenera con `supabase gen types typescript`
// una vez aplicadas las migraciones a un proyecto real.
export type Database = Record<string, never>;
